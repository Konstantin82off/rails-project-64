# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# Temporary files generated by your text editor or operating system
# belong in git's global ignore instead:
# `$XDG_CONFIG_HOME/git/ignore` or `~/.config/git/ignore`

# Ignore bundler config.
/.bundle

# Ignore all environment files (including .env.local for local configs).
/.env*
/.env.local

# Ignore all logfiles and tempfiles (including Puma logs).
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep

# Ignore pidfiles, but keep the directory.
/tmp/pids/*
!/tmp/pids/
!/tmp/pids/.keep

# Ignore storage (uploaded files in development and any SQLite databases).
/storage/*
!/storage/.keep
/tmp/storage/*
!/tmp/storage/
!/tmp/storage/.keep

# Ignore compiled assets (including Webpack builds).
/public/assets
/app/assets/builds/*
!/app/assets/builds/.keep

# Ignore node_modules (NPM/Yarn dependencies).
/node_modules

# Ignore Yarn cache.
/.yarn
/.pnp.*

# Ignore database dump files (SQLite, PostgreSQL).
/db/*.sqlite3
/db/*.sqlite3-journal
/db/*.psql

# Ignore credentials and keys (encrypted files, API keys).
/config/*.key
/config/credentials.yml.enc
/config/master.key

# Ignore Docker build artifacts.
/.docker
/docker-compose.override.yml

# Ignore IDE/editor files.
/.idea
/.vscode
/*.sublime-project
/*.sublime-workspace

# Ignore OS-specific files.
*.swp
*.swo
.DS_Store
.directory
.cache

# Ignore build/test artifacts.
/coverage/
/spec/reports/
/tmp/cache/

# Ignore local database seeds (if using sample data).
/db/seeds_local.rb

# Ignore specific generated files (Rails CLI, etc.).
/bin/*
!/bin/spring
!/bin/bundle

# Ignore package-lock.json (if using npm).
/package-lock.json

# Ignore ESLint cache.
/.eslintcache
